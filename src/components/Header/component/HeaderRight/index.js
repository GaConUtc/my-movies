import classNames from 'classnames/bind';
import styles from './HeaderRight.scss';

const cx = classNames.bind(styles);
function HeaderRight() {
    return <div className={cx('header-right')}></div>;
}

export default HeaderLeft;
